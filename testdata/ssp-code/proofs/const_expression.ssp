proof Const_Expression {
    const n: Integer;
    const m: Integer;
    const prf: fn Bits(n),Bits(n) -> Bits(n);
    const enc: fn Bits(n),Bits(n),Bits(n) -> Bits(m);

    instance construction_const_expression = ConstructionConstExpression {
        params {
            n: n,
            m: m,
            prf: prf,
            enc: enc,

        }
    }

    assumptions {
    }


    gamehops {
        }
}

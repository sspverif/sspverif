package KeyIdeal {
    params{
	 n: Integer,
    }
    
    state {
       K: Table(Bits(n), Bits(n)),
    }
    oracle Set(k: Bits(n), h: Bits(n))  {
        if (K[h]==Bot) {
            K[h] <-$ Bits(n);
	}
    }
    
    oracle Get(h_: Bits(n)) -> Bits(n) {
        return  Unwrap(K[h_]);
    }

}

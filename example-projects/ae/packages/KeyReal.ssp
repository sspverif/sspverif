package KeyReal {
    const n: Integer;
    
    state {
       K: Table(Integer, Bits(n)),
    }
    oracle Set(k: Bits(n), h: Integer)  {
        if (K[h]==Bot) {
            K[h] <- Some(k);
	}
    }
    
    oracle Get(h: Integer) -> Bits(n) {
        return  Unwrap(K[h]);
    }

}
